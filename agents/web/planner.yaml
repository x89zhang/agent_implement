enabled: true
type: static
max_steps: 5
steps:
- Understand the configured repository target and issue goal.
- Gather issue context and repository context needed for an accurate response.
- Decide whether a repository write action is required and keep any change narrowly
  scoped.
- Post the final issue comment after enough context is gathered.
- Verify the returned GitHub result before final confirmation.
